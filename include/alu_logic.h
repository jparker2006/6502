#ifndef GATES_H
#define GATES_H

#include "bit_def.h"

bit and(bit a, bit b);
bit not (bit a);
bit nand(bit a, bit b);
bit or(bit a, bit b);
bit nor(bit a, bit b);
bit xor(bit a, bit b);

#endif // GATES_H
